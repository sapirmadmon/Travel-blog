<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <title>places</title>

    <link rel="stylesheet" href="/css/main.css">
    <link href='https://fonts.googleapis.com/css?family=Anton' rel='stylesheet'>
    <style>
        @import url('https://fonts.googleapis.com/css?family=Montserrat:400,800');
        .title {
            color: #ffff;
            font-family: "Montserrat", sans-serif;
            font-weight: bold;
            letter-spacing: 4px;
            font-size: 60px;
        }
        
        .sub_title {
            color: #ffff;
            font-family: "Montserrat", sans-serif;
        }
        
        .design_card {
            background-color: rgba(255, 255, 255, 0.3);
            -webkit-backdrop-filter: blur(5px);
            backdrop-filter: blur(5px);
            border-color: white;
            margin-left: 20px;
        }
        
        .btn_add {
            float: right;
            margin-right: 65px;
            margin-top: 10px;
        }
    </style>
</head>

<body class="mybody">
    <div class="container">
        <br>
        <a href="/places/new" class="btn btn-primary btn_add"> New Place</a>
        <h1 class="title">BLOG</h1>
        <h3 class="sub_title"> The most beautiful places I would like to visit </h3>
        <div class="row justify-content-md-center">
            <% places.forEach(place => { %>
                <div class="col-lg-5 card mt-4 me-2 design_card">
                    <div class="row p-0 b-0 ">
                        <div class="col-4 px-md-0">
                            <img src="/uploads/images/<%= place.img %>" class="card-img" style="width: 175px; height: 185px;">
                        </div>
                        <div class="col-7">
                            <div class="card-body">
                                <h4>
                                    <%= place.title %>
                                </h4>
                                <div class="card-subtitle text-muted mb-2">
                                    <%= place.createdAt.toLocaleDateString() %>
                                </div>
                                <div class="card-text mb-2">
                                    <%= place.description %>
                                        <br>
                                        <a href="places/<%= place.slug %>" class="text-white">Read more</a>

                                </div>

                                <a href="places/edit/<%= place.id %>" class="btn  btn-primary btn-sm">Edit</a>
                                <form action="/places/<%= place.id %>?_method=DELETE" method="POST" class="d-inline">
                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>

                <% }) %>

        </div>

    </div>
</body>

</html>